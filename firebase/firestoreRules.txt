rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // This rule allows anyone with your Firestore database reference to view, edit,
    // and delete all data in your Firestore database. It is useful for getting
    // started, but it is configured to expire after 30 days because it
    // leaves your app open to attackers. At that time, all client
    // requests to your Firestore database will be denied.
    //
    // Make sure to write security rules for your app before that time, or else
    // all client requests to your Firestore database will be denied until you Update
    // your rules
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 12, 17);
    }
  }
}
// Instructions for setting up these rules:
// 1. Go to Firebase Console
// 2. Navigate to Firestore Database
// 3. Click on the "Rules" tab
// 4. Copy and paste the rules above (excluding this comment section)
// 5. Click "Publish" to apply the rules

// Rule Explanation:
// - Users can only read their own predictions
// - Admin (admin@example.com) can read all predictions
// - Users can create predictions with their own userId
// - Users can update and delete their own predictions
// - Admin has full access to all predictions
// - Admin can query all predictions for the admin dashboard
